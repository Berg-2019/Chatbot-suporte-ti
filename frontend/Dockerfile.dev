# Frontend Dockerfile.dev - Modo Desenvolvimento com Hot Reload
FROM node:22-alpine

WORKDIR /app

# Copiar package.json
COPY package*.json ./

# Instalar dependências (incluindo devDependencies)
RUN npm install

# Expor porta
EXPOSE 3000

# Iniciar em modo dev com hot reload
CMD ["npm", "run", "dev"]
